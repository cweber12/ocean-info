name = "movebank-worker"
main = "src/index.ts"
compatibility_date = "2026-01-01"

[vars]
MOVEBANK_BASE_URL = "https://www.movebank.org"
# Comma-separated Movebank study IDs. Replace placeholders with real IDs your account can access.
MOVEBANK_STUDY_IDS = "1027467132"
MAX_DAYS_BACK = "30"
MAX_RADIUS_KM = "100"

# Add routes in your Cloudflare dashboard or uncomment and set these values.
# routes = [
#   { pattern = "your-domain.com/api/movebank/*", zone_name = "your-domain.com" },
#   { pattern = "your-domain.com/api/weather/*", zone_name = "your-domain.com" },
# ]
